% Generated by roxygen2: do not edit by hand
% Please edit documentation in ./<text>
\name{lpr_extract_notes}
\alias{lpr_extract_notes}
\title{Extract Stata variable notes and characteristics}
\usage{
lpr_extract_notes(data, note_ids = NULL, include_dataset = FALSE)
}
\arguments{
\item{data}{A data frame with an `expansion.fields` attribute, or that
attribute's list of three-element character vectors.}

\item{note_ids}{Optional character vector of identifiers to retain.}

\item{include_dataset}{Include characteristics belonging to `_dta`.}
}
\value{
A data frame with character columns `variable_name`, `note_id`, and
  `note_value`. These columns are also present when no notes are available.
}
\description{
Returns the original characteristic identifiers, without assuming that a
particular note number is a question or that a variable label is a wording.
}
\examples{
toy <- data.frame(x = 1)
attr(toy, "expansion.fields") <- list(c("x", "note1", "Question?"))
lpr_extract_notes(toy)
}
